# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.28.1
python-multipart==0.0.19

# --- Your fine-tuned DistilBERT model ---
transformers==4.46.3
torch==2.5.1

# --- Open-source LLM via LangChain + Hugging Face Inference Providers ---
# NOTE: huggingface-hub>=0.27 / langchain-huggingface>=0.2 are required to
# route through router.huggingface.co. Older pins (e.g. huggingface-hub
# 0.26.x) still call the retired api-inference.huggingface.co host and will
# fail with a DNS resolution error on every LLM request.
langchain==0.3.27
langchain-core==0.3.79
langchain-huggingface==0.3.1
huggingface-hub==0.35.3

# --- Testing ---
pytest==8.3.4
